# name of profile in aws config/credentials
export AWS_PROFILE=

# aws region where you are going to deploy stuff
export AWS_REGION=

# url for the api, get from output of deployment, easy to use in scripts
export API_URI=https://example.execute-api.ap-southeast-2.amazonaws.com/dev
